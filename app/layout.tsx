import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { cookies } from "next/headers";
import ThemeToggleButton from "./components/theme-toggle-button";

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default async function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  let colorTheme = (await cookies()).get("color-theme");

  console.log("This is the color theme I got back", colorTheme);

  let theme;
  if (colorTheme) {
    theme = colorTheme.value;
  } else {
    theme = "light";
  }

  return (
    <html lang="en" data-theme={theme}>
      <head>
        <title>bubbles</title>
        <link
          href="https://api.fontshare.com/v2/css?f[]=satoshi@300,301,400,401,500,501,700,701,900,901&display=swap"
          rel="stylesheet"
        ></link>
      </head>
      <body>{children}</body>
    </html>
  );
}
